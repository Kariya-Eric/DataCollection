1、任务列表
	a.列表分页
	c.任务状态字段
	d.条件查询 schoolYear 字段
	e.list排序(添加任务顺序)
	f.根据统计时间排序单页排序还是列表排序，列表排序需要接口
	
2、	添加任务第一步
	a.任务类型，任务名称校验
	b.保存/下一步添加任务草稿，percentage,enableFlag 默认值 0

3、添加任务第二步
	a.表单合集下拉框  不包含pageBean接口
	b.获取表单列表接口   对应的状态和备注字段，统计截止时间,
	c.配置统计时间逻辑 -> 添加一条数据 修改数据？

4、添加任务第三部
	a、
	
5.添加任务第四部
	a.启动任务接口存在问题？